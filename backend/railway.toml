[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Railway polls this endpoint after each deployment. Must return 2xx within healthcheckTimeout seconds.
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
